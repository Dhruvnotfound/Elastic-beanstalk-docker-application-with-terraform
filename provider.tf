terraform {
  required_providers {
    aws = {
      source = "hashicorp/aws"
    }
  }
}

provider "aws" {
  region                   = "us-east-1"
  shared_credentials_files = ["~/.aws/credentials"] //credential provided through the vs code aws extension profile
  profile                  = "vscode_awskey"
}
